<% if(success && success.length){ %>
    <div class="alert alert-success alert-dismissible fade show col-6 offset-3" role="alert">
        <%= success %>
            <button type="button" class="btn-close " data-ds-dismiss="alert" aria-label="close"></button>
    </div>

    <%} %>
<% if(del && delete.length){ %>
    <div class="alert alert-danger alert-dismissible fode show col-6 offset-3">
        <%= delete %>
            <button 
            type="button"
            class="btn-close"
            data-ds-dismiss="alert"
            aria-label="close">

        </button>
    </div>
<%} %>

<% if(error && error.length){%>
    <div class="alert alert-danger alert-dismissible fode show col-6 offset-3">
        <%= error %>
            <button 
            type="button"
            class="btn-close"
            data-ds-dismiss="alert"
            aria-label="close">

        </button>
    </div>
<%}%>